1. Get the catalog JSON file.											Check
	-Use Requests to get the JSON.  									Check
	-"Optimize" the process. 											Check
	-Pretty-print the JSON file. 										Check
2. Scrape that JSON file to find our preferred threads.					Check
	-if preferred thread then store in database according to table		Check
3. Pipe that data into database.db (ID, thread number, content-type).	Check
4. Load the preferred threads into vars by ID and thread number.
5. Get the webms ONLY (save the filenames as well).
	-Don't overwrite if we already have it downloaded.
6. Arrange them into folders by content-type (eg. YLYL, YGYL, Slav, etc).
